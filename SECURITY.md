# Security Policy

## 支持范围

OpenQuantApp 目前处于早期开源阶段，只对主分支的最新版本提供安全修复。旧的
commit、个人 fork 和未经维护的部署版本不在支持范围内。

| 版本 | 安全支持 |
| --- | --- |
| 主分支最新版本 | 支持 |
| 历史版本与第三方 fork | 不保证 |

## 报告安全漏洞

请不要在公开 Issue、Discussion、Pull Request 或日志中披露漏洞细节。

优先使用 GitHub 仓库的 **Private vulnerability reporting** 功能提交报告。如果
该功能暂未启用，请通过仓库维护者的 GitHub 主页请求一个私下沟通渠道；公开
消息中只说明需要报告安全问题，不要附带复现步骤或敏感信息。

一份有帮助的报告通常包括：

- 受影响的版本、commit 和组件；
- 漏洞影响及可能的攻击条件；
- 最小复现步骤或概念验证；
- 已脱敏的日志、配置和请求样例；
- 已知的缓解措施或修复建议。

请勿发送真实 API Key、Token、Cookie、数据库、个人投资数据或其他第三方
凭据。项目维护者会尽力确认、评估并协调修复与披露，但当前不承诺固定响应
时限。

## 安全问题示例

适合按安全漏洞报告的问题包括：

- 凭据被写入日志、页面或错误响应；
- 可导致未授权数据访问的路径遍历、注入或请求伪造；
- 公开部署中的身份验证或权限绕过；
- 可被实际利用的依赖漏洞；
- 不安全的默认网络暴露或敏感文件读取。

普通市场数据偏差、第三方 API 中断、模型幻觉和指标计算缺陷通常属于常规 Bug，
可以在确认不含敏感信息后提交公开 Issue。

## 凭据泄露处理

如果 API Key 或 Token 已经出现在截图、日志、commit 或公开页面中，应立即：

1. 在对应服务商处撤销或轮换凭据；
2. 停止暴露该凭据的应用实例；
3. 检查相关服务的访问日志和异常费用；
4. 清理仓库或制品中的敏感内容；
5. 使用新凭据重新部署，并验证旧凭据已经失效。

仅从 Git 历史中删除字符串不能使已经泄露的凭据重新安全，轮换凭据始终是
首要步骤。

## 部署建议

OpenQuantApp 默认面向个人投研和可信网络环境。公开部署时，使用者应自行配置
身份验证、TLS、反向代理、网络访问控制和密钥管理，并及时更新依赖。不要把
Streamlit、数据库、ArcticDB 或本地 Ollama 端口直接暴露到公网。
